next-version: 1.3.0
branches:
  main:
    regex: ^master$|^main$
    mode: ContinuousDeployment
    label: ""
  develop:
    regex: ^dev$
    mode: ContinuousDeployment
    label: preview
    increment: Minor
