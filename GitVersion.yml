next-version: 1.2.0
branches:
  main:
    regex: ^master$|^main$
    mode: ContinuousDeployment
    label: ""
  develop:
    regex: ^dev$
    mode: ContinuousDeployment
    label: preview
    increment: Minor
